[package]
name = "cpi-caller"
version = "0.1.0"
edition = "2021"

[features]
bpf-entrypoint = []

[lib]
crate-type = ["cdylib", "lib"]
name = "cpi_caller"

[dependencies]
pinocchio = "0.9.0"
pinocchio-log = "0.5.0"
